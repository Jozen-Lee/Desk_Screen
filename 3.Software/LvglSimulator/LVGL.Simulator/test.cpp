
#include "test.h"

lv_obj_t* two;
lv_obj_t* one;
void lv_holo_cubic_gui(void) {
  // static lv_style_t default_style;
  // lv_style_init(&default_style);
  // lv_style_set_bg_color(&default_style, LV_STATE_DEFAULT, LV_COLOR_BLACK);
  // lv_style_set_bg_color(&default_style, LV_STATE_PRESSED, LV_COLOR_GRAY);
  // lv_style_set_bg_color(&default_style, LV_STATE_FOCUSED, LV_COLOR_BLACK);
  // lv_style_set_bg_color(&default_style, LV_STATE_FOCUSED | LV_STATE_PRESSED,
  // lv_color_hex(0xf88));

  // lv_obj_add_style(lv_scr_act(), &default_style, LV_BTN_PART_MAIN);
  // scr = lv_obj_create(NULL);

  // lv_obj_t* img = lv_img_create(scr);
  // lv_img_set_src(img, &logo);
  // lv_obj_set_x(img, 0);
  // lv_obj_set_y(img, -50);
  // lv_obj_set_align(img, LV_ALIGN_CENTER);
  // lv_obj_add_flag(img, LV_OBJ_FLAG_ADV_HITTEST);     /// Flags
  // lv_obj_clear_flag(img, LV_OBJ_FLAG_SCROLLABLE);      /// Flags

  // lv_disp_load_scr(scr);

  // btn1 = lv_btn_create(lv_scr_act());
  // lv_obj_align(btn1, LV_DIR_ALL, 0, 0);

  // btn2 = lv_btn_create(lv_scr_act());
  // lv_obj_align(btn2, LV_DIR_TOP, 0, 80);

  // btn3 = lv_btn_create(lv_scr_act());
  // lv_obj_align(btn3, LV_DIR_TOP, 0, 150);

  // btn4 = lv_btn_create(lv_scr_act());
  // lv_obj_align(btn4, LV_DIR_TOP, 0, 200);

  //one = lv_obj_create(lv_scr_act());  // 在默认屏上创建obj对象
  //lv_obj_t* label = lv_label_create(one);       // 创建label
  //lv_obj_set_size(one, LV_HOR_RES, LV_VER_RES);  // 设置到屏幕大小
  //lv_obj_align(label, LV_ALIGN_CENTER, 0, 0);    // label居屏幕中心对齐
  //lv_obj_set_style_bg_color(one, lv_color_hex(0x0000FF), LV_PART_MAIN);
  //lv_label_set_text(label, "ONE");  // label显示ONE

  //two = lv_obj_create(NULL);  // 在默认屏上创建obj对象
  //label = lv_label_create(two);         // 创建label
  //lv_obj_set_size(one, LV_HOR_RES, LV_VER_RES);  // 设置到屏幕大小
  //lv_obj_align(label, LV_ALIGN_CENTER, 0, 0);    // label居屏幕中心对齐
  //lv_obj_set_style_bg_color(two, lv_color_hex(0x00FF000), LV_PART_MAIN);
  //lv_label_set_text(label, "TWO");  // label显示ONE

  //lv_scr_load_anim(two, LV_SCR_LOAD_ANIM_FADE_ON, 500, 1000, true);

  //  lv_obj_t* btn = lv_btn_create(NULL);
  //lv_obj_align(btn, LV_ALIGN_CENTER, 0, 0);
  //lv_obj_set_style_bg_color(lv_scr_act(), lv_color_hex(0xFFFFFF), LV_PART_MAIN);
  //lv_obj_t* btn = lv_btn_create(lv_scr_act());
  //lv_obj_set_style_bg_color(btn, lv_color_hex(0x000000), LV_PART_MAIN);
  //lv_obj_align(btn, LV_ALIGN_CENTER, 0, 0);


}
